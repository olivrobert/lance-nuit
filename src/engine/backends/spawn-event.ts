// The shape lives in `contracts/backends/`: backend `types.ts` files sit in the
// public declaration graph, and the published surface must be able to type them.
export type { BackendSpawnEvent } from "../../contracts/backends/spawn-event.js";
