// The terminal screen is one screen of the shell, and `TerminalScreen` is the
// only way in: the pane is a part of it and is never mounted on its own.

export { TerminalScreen } from "./TerminalScreen.js";
